# JOBS
SPA ad management application built on PHP (Laravel) and React (TypeScript).

It includes features user registration, authentication, and comprehensive ad management (CRUD). The backend is equipped with rate limiting, CORS, CSP, input validation and sanitization, and JWT-based authentication. The entire stack is containerized with Docker Compose and includes a CI/CD pipeline for streamlined deployment and testing."

